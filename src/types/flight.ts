export type FlightSearch={origin:string;destination:string;departureDate:string;returnDate?:string;passengers:number;cabin:'Econômica'|'Premium'|'Executiva';baggage:boolean;directOnly:boolean;flexibility:number;highSeason:boolean};
export type FlightOffer={id:string;airline:string;flightNumber:string;origin:string;destination:string;departure:string;arrival:string;duration:string;stops:number;price:number;originalPrice:number;baggageIncluded:boolean;score:number;source:string;tag:'Melhor preço'|'Melhor custo-benefício'|'Mais rápido'};
export type SearchHistoryItem={id:string;searchedAt:string;search:FlightSearch;resultCount:number;lowestPrice:number};
